// function add(x, y) {
//     return x + y;
// }
//here function is stored in a variable.
//and it is loaded only when the code is loaded.
//and can be used as argument to another functions.
 
const add = function (x, y) {
    return x + y;
}

//to call we use
//add(2,3)